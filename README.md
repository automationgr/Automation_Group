# automation-group

Public website for Automation Group Ltd — a Kigali, Rwanda based industrial automation
and engineering systems integrator. Static HTML/CSS/JS site with content managed via
the companion Admin Portal (`../Admin_Portal`); falls back to the static markup in this
folder when the Admin Portal API is unreachable.